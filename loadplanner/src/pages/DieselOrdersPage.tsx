/**
 * @deprecated This page has been replaced by `OrdersPage`, which hosts both the
 * Diesel Orders and Truck Stops tabs. This file is no longer routed and can be
 * safely deleted. It re-exports `OrdersPage` only to avoid leaving stale,
 * divergent page logic behind.
 */
export { default } from "./OrdersPage";